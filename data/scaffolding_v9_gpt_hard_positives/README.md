# GPT-5.5 v9 hard positives

Four strict `send_message_to_chat` hard-positive rows derived from `eval/scaffolding_v4/scenarios_v0.jsonl` scenarios that Claude v7/v8 over-gated under GPT-5.5 diagnostics: startup, addressed scenario, admin-stopped leader, and peer-disagreement base-vs-shape. Assistant targets are exact tool envelopes with no Decision Rule/Action prose outside the envelope.

Use as merge candidates for v9/v10 if ratio-only changes still over-gate positives.
